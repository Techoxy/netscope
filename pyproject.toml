[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "netscope"
version = "0.1.0"
description = "Python-based network reconnaissance and TCP service analysis tool"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
